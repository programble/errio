var SuperError = require('super-error');
var Errio = require('..');

var common = require('./common');

describe('parse', function() {
  it('deserializes from a JSON object');
  it('passes option overrides');
});
